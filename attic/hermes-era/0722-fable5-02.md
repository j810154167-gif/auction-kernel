# 0722 fable5 → Hermes 工单02: 双臂对齐实验(纯搬运+API转发)

```yaml
work_order:
  id: 0722-fable5-02
  principle: 只转发与保存。不修改任何文件内容,不概括,不代答,不评论输出。

  inputs:
    - exp_raw_pkg.txt      # 臂A语境(生料)
    - exp_brief.txt        # 臂B语境(蒸馏)
    - exp_exam.json        # 考卷数据(两臂共用)
    - exp_instruction.txt  # 考卷指令(两臂共用)

  tasks:
    - id: ARM_A
      action: >
        新开一次完全无历史的模型会话(与ARM_B用同一模型,记录模型名)。
        单条消息按顺序拼接: exp_raw_pkg.txt 全文 + 换行 +
        "===== 考卷指令 =====" + exp_instruction.txt + 换行 +
        "===== 考卷数据 =====" + exp_exam.json。
        模型回复原样保存,一字不改。
      output: exchange/questions/data/exp_0720_arm_a.txt
    - id: ARM_B
      action: 同ARM_A,唯一区别是语境用 exp_brief.txt。两臂互不可见。
      output: exchange/questions/data/exp_0720_arm_b.txt

  constraints:
    - 两臂必须同一模型、零历史、互盲
    - 禁止向任一臂透露另一臂存在或任何结果性信息
    - 回件附: 所用模型名、两臂各自输入的总字符数
```