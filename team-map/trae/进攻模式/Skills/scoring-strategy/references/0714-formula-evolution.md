# Scoring Formula Evolution — 0714

## v20260709-1 (0625, 已弃用)

```
score = chg × 3 - max(0, dev-7) × 2 + min(vol/10000, 5) + vol_bonus - penalty × 0.4
```

问题: ×3 ×2 无市场逻辑, 0625试出来的数字后在0714修正数据后崩盘。

## v20260714 (当前)

```
score = chg - max(0, dev-7) + min(vol/10000, 5) + vol_bonus - penalty × 0.4
```

移除了 ×3 ×2 乘法系数。回归自然梯度——竞涨幅主导, 偏差惩罚不再踩踏。断崖消失(★1与★2间距从25.6→11.8)。

## close_t1 vs prev_close 陷阱

池里 `prev_close` = T-2收盘(建池算涨幅的前一日), `close_t1` = 昨日收盘。
D1的open_chg_pct必须用close_t1。误用prev_close导致水发燃气0714: +19.7%→+8.8%, 得分66.1→33.3。

## 六灯讨论 (共识，未编码)

六盏灯（价/量/题材/筹码/风控/赔率）→ 砍到三盏：
- 价灯 = L0 🟢🔴 已覆盖 → 不重复
- 风控灯 = 盘后更合适 → 不占D1带宽
- 题材/筹码 = 未接入 → 以后再说

D1 当前灯：L0 + 量 + 赔率。风控归盘后复盘。

## 列讨论 (共识，未编码)

偏差和得分准备砍：
- 偏差：涨停票偏差天然大，无区分度
- 得分：公式黑盒，排序已在 ★ 里完成

保留：★ | symbol | name | price | chg%(vs VWAP) | VWAP(L0)

## 55轮修改根因

Agent跳过仿写直接改格式，每轮自以为"优化"。终端stdout的markdown vs 回复渲染的markdown —— 格式相同但UI表现不同。Agent在终端里看表格、以为不对、反复改。正确流程：先仿写原文→用户确认→再改。已写入协议。
